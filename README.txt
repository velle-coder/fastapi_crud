## Git push in own branch
     `git init`
      `git remote add origin `link of repo``
      `git add .`
       `git status`
        `git commit -m "Features"`
        `git push --set-upstream origin filename`


## to run the code

    `uvicorn filename:app --reload`